# Semantic Resume Matcher

An NLP-based system that semantically matches resumes with job descriptions using **BERT embeddings**.  
This tool helps recruiters and job seekers by providing intelligent, context-aware matching beyond keyword search.

---

## 🚀 Features
- Uses **BERT embeddings** for semantic similarity.
- Matches resumes with job descriptions based on meaning, not just keywords.
- Interactive **Streamlit dashboard** for uploading resumes and job descriptions.
- Provides similarity scores and highlights relevant matches.

---

## 📦 Installation

Clone the repository and install dependencies:

```bash
git clone https://github.com/your-username/semantic-resume-matcher.git
cd semantic-resume-matcher
pip install -r requirements.txt
